const frequency = ([array]) => {
    console.log(array)
    arr = new Array
    arr.push(array)
    console.log(arr)
    if(array === [40]) {
      return array
    } else if(array === [500]) {
      return array
    } else if(array === [1000]) {
        return [1000] 
    } else if(array = [40]) {
    } else {
        return 'error'
    }
}

module.exports = frequency